# 生成三角形之间的邻接矩阵

## 边数组存储使用该边的三角形的序号
遍历每个三角形
    遍历三角形的每条边
        将三角形的序号推入二维数组：边（顶点序号x1,顶点序号x2）、边（顶点序号x2,顶点序号x1）

## 三角数组存储与该三角相邻的三角形的序号
遍历每个三角形
    遍历三角形的每条边
        遍历该边的边数组
            将边数组中不是本三角的三角序号读入（疑问：else if为什么要判断-1），同时存储中点坐标与该边